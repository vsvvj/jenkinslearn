pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
        stage('checkout'){
            steps {
                git url: 'https://github.com/vsvvj/jenkinslearn.git', branch: 'main'
                sh 'ls -lrt'
            }
        }
    }
}

