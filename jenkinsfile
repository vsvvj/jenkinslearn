pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
        stage('checkout'){
            steps {
                git url: 'https://github.com/vsvvj/jenkinslearn.git', branch: 'main'
                sh 'ls -lrt'
            }
        }
        stage('setup') {
            steps {
                sh "pip install -r requirements.txt"
            }
        }
    }
}

